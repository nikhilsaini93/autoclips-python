fastapi==0.141.1
uvicorn[standard]==0.52.3
python-dotenv==1.2.2
opencv-python==4.10.0.84
numpy==2.5.2
faster-whisper==1.2.1
yt-dlp==2026.8.19
google-genai==2.18.1
python-multipart==0.0.32
python-telegram-bot==22.8
slowapi==0.1.10
pydantic-settings==2.15.0
google-api-python-client==2.200.0
google-auth-oauthlib==1.4.1
google-auth-httplib2==0.4.2
requests==2.31.0
# Hinglish subtitles: Devanagari -> Latin romanization (pure python, offline).
indic-transliteration==2.3.82
# T4 face pipeline: YOLOv8n-face on CUDA (falls back to YuNet/Res10 if missing).
# NOTE: Colab already ships CUDA torch — install with `pip install -r requirements.txt`
# and pip will reuse it; do NOT force a torch reinstall unless versions clash.
ultralytics==8.3.40
